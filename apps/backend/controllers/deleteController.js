export async function deleteObsoleteProducts(shopDomain) {
  // Lógica para deletar do banco ou da Shopify
  console.log(`Deletando produtos da loja: ${shopDomain}`);
  return 10; // retorno fictício (quantos foram apagados)
}